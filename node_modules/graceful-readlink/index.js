var fs = require('fs')
  , lstat = fs.lstatSync;

/**
 * Description
 * @method readlinkSync
 * @param {} p
 * @return 
 */
exports.readlinkSync = function (p) {
  if (lstat(p).isSymbolicLink()) {
    return fs.readlinkSync(p);
  } else {
    return p;
  }
};


